// Multi-script font-preview samples. These are intentionally identical in
// every locale (they exist to exercise CJK glyph variants), so they live as
// consts here rather than in the per-locale message tables.
const appFontPreviewText = 'Aa 123 中文 日本語 한국어';
const appMonospaceFontPreviewText = 'final count = 123;';
